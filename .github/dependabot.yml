version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    # Only open PRs for security updates (not every minor bump)
    groups:
      security-patches:
        applies-to: security-updates
        patterns:
          - "*"
